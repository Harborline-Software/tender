# Vendored Harborline brand assets

Copied verbatim from the private `brand` repository
(`brand/assets/marks/sun-wave/`, commit `028a639` — no release tag existed yet;
pin a tag on the next sync). Consumer contract (brand/README.md): consume named
variants (`on-light` / `on-dark` / `small`), never recolor via CSS inheritance,
never edit these files — change the brand source and re-copy.

- `harborline-mark-on-dark.svg`  — white-wave mark for dark surfaces
- `harborline-mark-on-light.svg` — cobalt-wave mark for light surfaces
- `harborline-mark-small.svg`    — favicon / <32px compact cut
